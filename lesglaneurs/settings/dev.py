from settings import *

ALLOWED_HOSTS = ['127.0.0.1',
                 'localhost']

DEBUG=True
TEMPLATE_DEBUG = True

LOGGING = {
    'version': 1,
    'handlers': {
        'console':{
            'level':'DEBUG',
            'class':'logging.StreamHandler',
        },
    },
    'loggers': {
        'django.request': {
            'handlers':['console'],
            'propagate': True,
            'level':'DEBUG',
        }
    },
}
